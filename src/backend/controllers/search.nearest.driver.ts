/**
 * SearchNearestDriver class locate the nearest driver
 * in respective of passengers current location.
 */

interface ISearchNearestDriver {
    
} 

class SearchNearestDriver {
    constructor(coords) {
        console.log(coords);
    }
}